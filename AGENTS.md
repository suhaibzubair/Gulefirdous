# AGENTS.md

## Project overview

**Gulefirdous** is currently an empty starter repository. The only tracked file is `README.md` (title: "Gulefirdous"). There is no application code, dependency manifests, Docker configuration, CI, or test suite yet.

## Development commands

No project-specific commands exist until application code is added. When a stack is introduced, document the standard commands here (install, lint, test, dev server).

## Cursor Cloud specific instructions

- **Repository state:** This repo has no `package.json`, `requirements.txt`, `Makefile`, `docker-compose.yml`, or other setup files. There is nothing to install and no service to start.
- **Update script:** The VM update script is a no-op (`true`) because there are no dependencies to refresh on startup.
- **Lint / test / run:** Not applicable until code is added. Re-run environment discovery after the first application commit.
- **Git:** The default branch is `main`; the remote is `origin` on GitHub (`suhaibzubair/Gulefirdous`).
